# Chaukas SDK Configuration
# Copy this file to .env and replace with your actual values

# ========================================
# REQUIRED - Chaukas Platform Configuration
# ========================================

# Your Chaukas tenant identifier
CHAUKAS_TENANT_ID=your-tenant-id-here

# Your Chaukas project identifier  
CHAUKAS_PROJECT_ID=your-project-id-here

# ========================================
# OUTPUT CONFIGURATION
# ========================================

# Output mode: "api" (send to Chaukas platform) or "file" (write to local file)
CHAUKAS_OUTPUT_MODE=file

# For API mode - required
CHAUKAS_ENDPOINT=https://api.chaukas.com
CHAUKAS_API_KEY=your-api-key-here

# For file mode - required when OUTPUT_MODE=file
# CHAUKAS_OUTPUT_FILE=events.jsonl

# ========================================
# OPTIONAL - Performance Tuning
# ========================================

# Event batch size - number of events per batch (default: 20, reduced from 100 for better performance)
CHAUKAS_BATCH_SIZE=20

# Maximum batch size in bytes (default: 262144, i.e., 256KB)
# Prevents high memory usage errors by limiting payload size
CHAUKAS_MAX_BATCH_BYTES=262144

# Minimum batch size for retry (default: 1)
# When retrying after errors, batches won't be split below this size
CHAUKAS_MIN_BATCH_SIZE=1

# Enable adaptive batching - automatically adjusts batch size based on payload (default: true)
# When enabled, batches are sent when either count or size limit is reached
CHAUKAS_ENABLE_ADAPTIVE_BATCHING=true

# Flush interval in seconds (default: 5.0)
CHAUKAS_FLUSH_INTERVAL=5.0

# Request timeout in seconds (default: 30.0)
CHAUKAS_TIMEOUT=30.0

# Git branch name for context tracking (optional)
CHAUKAS_BRANCH=main

# Comma-separated tags for filtering events (optional)
CHAUKAS_TAGS=production,agent-monitoring

# ========================================
# FRAMEWORK-SPECIFIC CONFIGURATION
# ========================================

# OpenAI API key (required for OpenAI Agents and CrewAI)
OPENAI_API_KEY=your-openai-api-key-here

# Google API key (required for Google ADK)
GOOGLE_API_KEY=your-google-api-key-here

# Disable CrewAI telemetry to prevent "Service Unavailable" errors
# CrewAI sends its own telemetry to their servers which can cause 503 errors
CREWAI_DISABLE_TELEMETRY=true

# ========================================
# EXAMPLE USAGE
# ========================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. Load in your application or shell:
#    - Python: python-dotenv package
#    - Shell: source .env
#    - Docker: --env-file .env

# ========================================
# TESTING CONFIGURATION
# ========================================
# For testing event generation without API:
# CHAUKAS_OUTPUT_MODE=file
# CHAUKAS_OUTPUT_FILE=test_events.jsonl
# CHAUKAS_TENANT_ID=test-tenant
# CHAUKAS_PROJECT_ID=test-project